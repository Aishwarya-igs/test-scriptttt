{
  "name": "tfc-admin-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone admin app for onboarding clients and viewing a consolidated, read-only overview of each client's Automation Dashboard history. Fully separate from dashboard/ — never imports it, never writes to it.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
